# `df` (Disk Free)

The `df` command reports the amount of available disk space being used by file systems.

- **Standard usage**: `df -h`
    
- The `-h` flag stands for "human-readable," which formats the output into Megabytes (MB) and Gigabytes (GB) rather than raw block sizes, making it much easier to read.